services:
  - type: web
    name: pdfyako-backend
    env: docker
    rootDir: backend
    plan: free
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
